
初始化版本库时会发生什么情况？为什么你需要这样做？

	会新建一个.git文件夹，但是不会主动创建一个commit。初始化时不主动创建commit一是因为可能不需要commit整个文件夹，二是因为主动创建commit后无法为此次commit提供信息。


暂存区与工作目录和版本库有何不同？你认为它具有什么价值？

	暂存区是工作目录和版本库中间的一块缓存区域，通过它可以灵活的创建单个文件或多个文件的commit版本。